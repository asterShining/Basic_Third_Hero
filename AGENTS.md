# 全局编程指令与行为规范

## 1. 核心架构与设计哲学 (Core Philosophy)
- **语言与沟通风格**：对话与代码注释默认使用简体中文；通用技术术语、库名、变量名、协议字段名保持英文，以避免术语歧义和与代码实际命名脱节。
- **输出风格**：结论先行，表达简洁、专业、直接，禁止空话和无效铺垫，以降低沟通成本并减少执行歧义。
- **全局审视，绝不重复造轮子**：在编写任何新逻辑前，必须先扫描项目现有上下文。优先复用原生语言特性、标准库或项目中已有的工具类/函数。禁止编写与现有标准库功能重叠的自定义逻辑。
- **联合上下文分析**：分析问题时禁止只盯单个文件，必须结合关联文件一起看清入口、出口、依赖关系、调用方、被调用方、配置源、类型定义和测试覆盖，避免基于局部片段做错误判断。
- **先摸清链路再修改**：动手前必须明确实际工具链条与执行链路，包括数据流、调用链、构建链、脚本入口、生成产物、测试与发布路径；禁止在链路不明时凭猜测改代码，以避免改错层级或漏改上下游。
- **最小化修改 (Minimalist Intervention)**：严格限制代码变动范围。只修改实现当前需求所必需的最少代码行。严禁对无关代码进行“顺手重构”、重新缩进或大范围格式化。
- **直白编码，拒绝过度封装**：保持代码结构扁平化。除非某段逻辑在当前项目中可预见地会在 3 处以上被复用，否则直接内联编写。禁止为单一、简单的线性流程创建深层嵌套或冗余的抽象函数。

## 2. 代码编写与注释规范 (Code & Comments)
- **全覆盖级详细注释**：对于所有新增或修改的代码，必须逐行（或逐个最小逻辑单元）提供详尽的中文注释。
- **注释深度**：注释不能仅仅翻译代码（例如 `i++ // i加1` 是被禁止的），必须直接说明业务意图、设计动机、边界条件和规避风险的原因；不需要显式写出 `What`、`Why` 标题，但信息必须覆盖到位。
- **注释语言风格**：禁止使用 `What:`、`Why:` 一类标签式注释。所有注释必须使用专业、自然、完整的中文工程表达，直接写清“这段逻辑做什么、为什么这样做、要避免什么问题”，避免模板化标签和机械句式。
- **注释布局要求**：优先使用函数前说明、代码块前说明和必要的行内注释三种自然布局；避免同一逻辑被拆成多条重复注释，也避免只删标签不重写导致语句生硬。
- **按技术栈自适应优化**：C/C++ 与 Embedded 场景优先保证实时安全，关键循环中禁止动态分配，并关注时序、内存对齐与拷贝成本；Go 与 TS/Vue（Electron）场景优先保证并发安全、符合语言习惯的错误处理和清晰的模块边界。

## 3. 约束清单 (Checklist before Output)
- [ ] 是否使用了现有轮子而不是自己手写？
- [ ] 是否联合查看了相关文件，而不是只看单个文件就下结论？
- [ ] 是否已经明确上下游调用链、数据流以及构建/测试工具链条？
- [ ] 代码变动是否已经精简到了极致？
- [ ] 是否存在只有一处调用却被提取出来的冗余函数？
- [ ] 是否所有新增或修改的核心逻辑都配有专业、自然、详细且不使用 `What/Why` 标签的中文注释？
