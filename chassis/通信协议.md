```c
// 命令码: 0x0001
[cite_start]// 说明: 比赛状态数据，1Hz 频率发送 [cite: 64, 80]
typedef _packed struct
{
    uint8_t game_type : 4;          [cite_start]// 比赛类型: 1-RMUC, 4-高校联盟赛3V3, 5-步兵对抗等 [cite: 80]
    uint8_t game_progress : 4;      [cite_start]// 当前比赛阶段: 0-未开始, 1-准备, 4-比赛中, 5-结算中 [cite: 80]
    uint16_t stage_remain_time;     [cite_start]// 当前阶段剩余时间 (单位: 秒) [cite: 80]
    uint64_t SyncTimeStamp;         [cite_start]// UNIX时间戳 (机器人连接NTP服务器后生效) [cite: 80]
} game_status_t;

// 命令码: 0x0002
[cite_start]// 说明: 比赛结果数据，比赛结束触发发送 [cite: 64, 86]
typedef _packed struct
{
    uint8_t winner;                 [cite_start]// 0-平局, 1-红方胜利, 2-蓝方胜利 [cite: 86]
} game_result_t;

// 命令码: 0x0003
[cite_start]// 说明: 机器人血量数据，3Hz 频率发送 [cite: 64, 92]
typedef _packed struct
{
    uint16_t ally_1_robot_HP;       [cite_start]// 己方1号英雄血量 [cite: 92]
    uint16_t ally_2_robot_HP;       [cite_start]// 己方2号工程血量 [cite: 92]
    uint16_t ally_3_robot_HP;       [cite_start]// 己方3号步兵血量 [cite: 92]
    uint16_t ally_4_robot_HP;       [cite_start]// 己方4号步兵血量 [cite: 92]
    uint16_t reserved;              [cite_start]// 保留位 [cite: 92]
    uint16_t ally_7_robot_HP;       [cite_start]// 己方7号哨兵血量 [cite: 92]
    uint16_t ally_outpost_HP;       [cite_start]// 己方前哨站血量 [cite: 92]
    uint16_t ally_base_HP;          [cite_start]// 己方基地血量 [cite: 92]
} game_robot_HP_t;

// 命令码: 0x0101
[cite_start]// 说明: 场地事件数据，1Hz 频率发送 [cite: 64, 106]
typedef _packed struct
{
    uint32_t event_data;            [cite_start]// 包含补给站、能量机关、高地占领等状态的位域 [cite: 106, 110]
} event_data_t;

// 命令码: 0x0104
[cite_start]// 说明: 裁判警告数据，触发发送 [cite: 64, 116]
typedef _packed struct
{
    uint8_t level;                  [cite_start]// 警告等级: 2-黄牌, 3-红牌, 4-判负 [cite: 116]
    uint8_t offending_robot_id;     [cite_start]// 违规机器人ID [cite: 116]
    uint8_t count;                  [cite_start]// 违规次数 [cite: 116]
} referee_warning_t;

// 命令码: 0x0105
[cite_start]// 说明: 飞镖发射相关数据，1Hz 频率发送 [cite: 64, 126]
typedef _packed struct
{
    uint8_t dart_remaining_time;    [cite_start]// 己方飞镖发射剩余时间 (单位: 秒) [cite: 126]
    uint16_t dart_info;             [cite_start]// 包含最近击中目标、累计击中次数、选定目标等 [cite: 126]
} dart_info_t;

// 命令码: 0x0201
[cite_start]// 说明: 机器人性能体系数据，10Hz 频率发送 [cite: 64, 134]
typedef _packed struct
{
    uint8_t robot_id;               [cite_start]// 本机器人ID [cite: 134]
    uint8_t robot_level;            [cite_start]// 机器人等级 [cite: 134]
    uint16_t current_HP;            [cite_start]// 当前血量 [cite: 137]
    uint16_t maximum_HP;            [cite_start]// 血量上限 [cite: 137]
    uint16_t shooter_barrel_cooling_value; [cite_start]// 每秒冷却值 [cite: 137]
    uint16_t shooter_barrel_heat_limit;    [cite_start]// 热量上限 [cite: 137]
    uint16_t chassis_power_limit;          [cite_start]// 底盘功率上限 [cite: 137]
    uint8_t power_management_gimbal_output : 1;  [cite_start]// 云台口输出状态 [cite: 137]
    uint8_t power_management_chassis_output : 1; [cite_start]// 底盘口输出状态 [cite: 137]
    uint8_t power_management_shooter_output : 1; [cite_start]// 发射机构口输出状态 [cite: 137]
} robot_status_t;

// 命令码: 0x0202
[cite_start]// 说明: 实时底盘缓冲能量和射击热量数据，10Hz 频率发送 [cite: 64, 153]
typedef _packed struct
{
    uint16_t reserved1;             [cite_start]// 保留位 [cite: 153]
    uint16_t reserved2;             [cite_start]// 保留位 [cite: 153]
    float reserved3;                [cite_start]// 保留位 [cite: 156]
    uint16_t buffer_energy;         [cite_start]// 缓冲能量 (单位: J) [cite: 156]
    uint16_t shooter_17mm_barrel_heat; [cite_start]// 17mm枪管实时热量 [cite: 156]
    uint16_t shooter_42mm_barrel_heat; [cite_start]// 42mm枪管实时热量 [cite: 156]
} power_heat_data_t;

// 命令码: 0x0203
[cite_start]// 说明: 机器人位置数据，1Hz 频率发送 [cite: 64, 167]
typedef _packed struct
{
    float x;                        [cite_start]// 位置x坐标 (单位: m) [cite: 167]
    float y;                        [cite_start]// 位置y坐标 (单位: m) [cite: 167]
    float angle;                    [cite_start]// 朝向角度 (单位: 度)，正北为0度 [cite: 167]
} robot_pos_t;

// 命令码: 0x0204
[cite_start]// 说明: 机器人增益和底盘能量，3Hz 频率发送 [cite: 64, 179]
typedef _packed struct
{
    uint8_t recovery_buff;          [cite_start]// 回血增益 (%) [cite: 176]
    uint16_t cooling_buff;          [cite_start]// 冷却增益 (直接值) [cite: 179]
    uint8_t defence_buff;           [cite_start]// 防御增益 (%) [cite: 179]
    uint8_t vulnerability_buff;     [cite_start]// 负防御增益 (%) [cite: 179]
    uint16_t attack_buff;           [cite_start]// 攻击增益 (%) [cite: 179]
    uint8_t remaining_energy;       [cite_start]// 剩余能量值反馈 [cite: 179]
} buff_t;

// 命令码: 0x0206
[cite_start]// 说明: 伤害状态数据，伤害发生后发送 [cite: 64, 182]
typedef _packed struct
{
    uint8_t armor_id : 4;           [cite_start]// 装甲模块/测速模块ID [cite: 182]
    uint8_t HP_deduction_reason : 4; [cite_start]// 扣血原因: 0-弹丸, 1-模块离线, 5-撞击 [cite: 182]
} hurt_data_t;

// 命令码: 0x0207
[cite_start]// 说明: 实时射击数据，弹丸发射后发送 [cite: 68, 191]
typedef _packed struct
{
    uint8_t bullet_type;            [cite_start]// 弹丸类型: bit1-17mm, bit2-42mm [cite: 191]
    uint8_t shooter_number;         [cite_start]// 发射机构ID: 1-17mm, 3-42mm [cite: 191]
    uint8_t launching_frequency;    [cite_start]// 射频 (单位: Hz) [cite: 191]
    float initial_speed;            [cite_start]// 初速度 (单位: m/s) [cite: 194]
} shoot_data_t;

// 命令码: 0x0208
[cite_start]// 说明: 允许发弹量，10Hz 频率发送 [cite: 68, 203]
typedef _packed struct
{
    uint16_t projectile_allowance_17mm; [cite_start]// 17mm弹丸允许量 [cite: 203]
    uint16_t projectile_allowance_42mm; [cite_start]// 42mm弹丸允许量 [cite: 203]
    uint16_t remaining_gold_coin;       [cite_start]// 剩余金币数 [cite: 203]
    uint16_t projectile_allowance_fortress; [cite_start]// 堡垒储备发弹量 [cite: 203]
} projectile_allowance_t;

// 命令码: 0x0209
[cite_start]// 说明: 机器人 RFID 状态，3Hz 频率发送 [cite: 68, 212]
typedef _packed struct
{
    uint32_t rfid_status;           [cite_start]// RFID检测位 bit0-31 [cite: 212, 214, 219]
    uint8_t rfid_status_2;          [cite_start]// RFID检测位扩展 [cite: 224]
} rfid_status_t;

// ==========================================
// 5. 场上机制与雷达同步数据 (0x020A - 0x020E)
// ==========================================

// 命令码: 0x020A
// 说明: 飞镖选手端指令数据，固定以 3Hz 频率发送
typedef _packed struct
{
    uint8_t dart_launch_opening_status; // 飞镖发射站状态: 1-关闭, 2-正在开启或关闭中, 0-已经开启
    uint8_t reserved;                   // 保留位
    uint16_t target_change_time;        // 切换击打目标时的比赛剩余时间 (单位: 秒)
    uint16_t latest_launch_cmd_time;    // 最后一次操作手确定发射指令时的比赛剩余时间 (单位: 秒)
} dart_client_cmd_t;

// 命令码: 0x020B
// 说明: 地面机器人位置数据，固定以 1Hz 频率发送
typedef _packed struct
{
    float hero_x;                       // 己方英雄位置x轴坐标 (单位: m)
    float hero_y;                       // 己方英雄位置y轴坐标 (单位: m)
    float engineer_x;                   // 己方工程位置x轴坐标 (单位: m)
    float engineer_y;                   // 己方工程位置y轴坐标 (单位: m)
    float standard_3_x;                 // 己方3号步兵位置x轴坐标 (单位: m)
    float standard_3_y;                 // 己方3号步兵位置y轴坐标 (单位: m)
    float standard_4_x;                 // 己方4号步兵位置x轴坐标 (单位: m)
    float standard_4_y;                 // 己方4号步兵位置y轴坐标 (单位: m)
    float reserved;                     // 保留位
    float reserved_2;                   // 保留位
} ground_robot_position_t;

// 命令码: 0x020C
// 说明: 雷达标记进度数据，固定以 1Hz 频率发送
typedef _packed struct
{
    uint16_t mark_progress;             // 按位标识对方或己方机器人是否被雷达标记/暴露易伤
} radar_mark_data_t;

// 命令码: 0x020D
// 说明: 哨兵自主决策信息同步，固定以 1Hz 频率发送
typedef _packed struct
{
    uint32_t sentry_info;               // 包含哨兵发弹量兑换、复活确认、花费等信息的位域
    uint16_t sentry_info_2;             // 包含脱战状态、允许发弹量剩余可兑换数、姿态等信息的位域
} sentry_info_t;

// 命令码: 0x020E
// 说明: 雷达自主决策信息同步，固定以 1Hz 频率发送
typedef _packed struct
{
    uint8_t radar_info;                 // 包含触发双倍易伤机会、加密等级及修改密钥权限等信息的位域
} radar_info_t;


// ==========================================
// 6. 机器人间与客户端交互数据 (0x0301 - 0x0311)
// ==========================================

// 命令码: 0x0301
// 说明: 机器人交互数据，发送方触发发送，频率上限为 30Hz
typedef _packed struct
{
    uint16_t data_cmd_id;               // 子内容 ID (如 0x0100 - 0x0121，用于控制小地图 UI 或自主决策)
    uint16_t sender_id;                 // 发送者 ID
    uint16_t receiver_id;               // 接收者 ID
    uint8_t user_data[112];             // 内容数据段，最大112字节
} robot_interaction_data_t;

// 命令码: 0x0302
// 说明: 自定义控制器与机器人交互数据，发送方触发发送，频率上限为 30Hz
typedef _packed struct
{
    uint8_t data[30];                   // 自定义数据段
} custom_robot_data_t;

// 命令码: 0x0303
// 说明: 选手端小地图交互数据，选手端触发发送
typedef _packed struct
{
    float target_position_x;            // 目标位置x轴坐标 (单位: m)
    float target_position_y;            // 目标位置y轴坐标 (单位: m)
    uint8_t cmd_keyboard;               // 云台手按下的键盘按键通用键值
    uint8_t target_robot_id;            // 对方机器人 ID
    uint16_t cmd_source;                // 信息来源 ID
} map_command_t;

// 命令码: 0x0305
// 说明: 选手端小地图接收雷达数据，频率上限为 5Hz
typedef _packed struct
{
    uint16_t hero_position_x;           // 英雄机器人x位置坐标 (单位: cm)
    uint16_t hero_position_y;           // 英雄机器人y位置坐标 (单位: cm)
    uint16_t engineer_position_x;       // 工程机器人x位置坐标 (单位: cm)
    uint16_t engineer_position_y;       // 工程机器人y位置坐标 (单位: cm)
    uint16_t infantry_3_position_x;     // 3号步兵机器人x位置坐标 (单位: cm)
    uint16_t infantry_3_position_y;     // 3号步兵机器人y位置坐标 (单位: cm)
    uint16_t infantry_4_position_x;     // 4号步兵机器人x位置坐标 (单位: cm)
    uint16_t infantry_4_position_y;     // 4号步兵机器人y位置坐标 (单位: cm)
    uint16_t reserved;                  // 保留位
    uint16_t reserved_2;                // 保留位
    uint16_t sentry_position_x;         // 哨兵机器人x位置坐标 (单位: cm)
    uint16_t sentry_position_y;         // 哨兵机器人y位置坐标 (单位: cm)
} map_robot_data_t;

// 命令码: 0x0306
// 说明: 自定义控制器与选手端交互数据 (模拟键鼠操作)，频率上限为 30Hz
typedef _packed struct
{
    uint16_t key_value;                 // 键盘按键1与按键2键值
    uint16_t x_position:12;             // 鼠标X轴像素位置
    uint16_t mouse_left:4;              // 鼠标左键状态
    uint16_t y_position:12;             // 鼠标Y轴像素位置
    uint16_t mouse_right:4;             // 鼠标右键状态
    uint16_t reserved;                  // 保留位
} custom_client_data_t;

// 命令码: 0x0307
// 说明: 选手端小地图接收路径数据，频率上限为 1Hz
typedef _packed struct
{
    uint8_t intention;                  // 意图: 1-到目标点攻击, 2-到目标点防守, 3-移动到目标点
    uint16_t start_position_x;          // 路径起点x轴坐标 (单位: dm)
    uint16_t start_position_y;          // 路径起点y轴坐标 (单位: dm)
    int8_t delta_x[49];                 // 路径点x轴增量数组 (单位: dm)
    int8_t delta_y[49];                 // 路径点y轴增量数组 (单位: dm)
    uint16_t sender_id;                 // 发送者 ID
} map_data_t;

// 命令码: 0x0308
// 说明: 选手端小地图接收机器人自定义消息，频率上限为 3Hz
typedef _packed struct
{
    uint16_t sender_id;                 // 发送者的ID
    uint16_t receiver_id;               // 接收者的ID
    uint8_t user_data[30];              // 字符 (utf-16格式编码)
} custom_info_t;

// 命令码: 0x0309
// 说明: 自定义控制器接收机器人数据，频率上限为 10Hz
typedef _packed struct
{
    uint8_t data[30];                   // 自定义数据
} robot_custom_data_t;

// 命令码: 0x0310
// 说明: 机器人发送给自定义客户端的数据，频率上限为 50Hz
typedef _packed struct
{
    uint8_t data[300];                  // 自定义数据
} robot_custom_data_2_t;

// 命令码: 0x0311
// 说明: 自定义客户端发送给机器人的自定义指令，频率上限为 75Hz
typedef _packed struct
{
    uint8_t data[30];                   // 自定义数据
} robot_custom_data_3_t;
```





